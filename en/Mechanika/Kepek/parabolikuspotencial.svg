<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 550 400" width="100%" max-width="550px">
  <!-- Explicit white background -->
  <rect width="100%" height="100%" fill="#ffffff"/>

  <defs>
    <!-- Arrowheads for the axes -->
    <marker id="arrow" viewBox="0 0 10 10" refX="10" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="#000" />
    </marker>
  </defs>

  <!-- Coordinate axes -->
  <path d="M 50 320 L 500 320" fill="none" stroke="#000" stroke-width="2" marker-end="url(#arrow)"/>
  <path d="M 275 350 L 275 40" fill="none" stroke="#000" stroke-width="2" marker-end="url(#arrow)"/>

  <!-- Axis labels -->
  <text x="510" y="340" font-family="Arial" font-size="16">x</text>
  <text x="285" y="50" font-family="Arial" font-size="16">Energy (E)</text>

  <!-- Total energy line (horizontal) - strictly between -A and +A -->
  <line x1="125" y1="120" x2="425" y2="120" stroke="#e60000" stroke-width="3"/>

  <!-- Parabola (Potential energy curve) -->
  <!-- Extended past the +-A points into the classically forbidden region -->
  <!-- The curve passes exactly through (125,120) and (425,120) -->
  <path d="M 100 48 Q 275 592 450 48" fill="none" stroke="#0055cc" stroke-width="3"/>

  <!-- Dashed lines for extreme positions (+A, -A) -->
  <line x1="125" y1="120" x2="125" y2="320" stroke="#666" stroke-width="2" stroke-dasharray="5,5"/>
  <line x1="425" y1="120" x2="425" y2="320" stroke="#666" stroke-width="2" stroke-dasharray="5,5"/>

  <!-- Axis ticks / labels -->
  <text x="115" y="345" font-family="Arial" font-size="16">-A</text>
  <text x="415" y="345" font-family="Arial" font-size="16">+A</text>
  <text x="260" y="345" font-family="Arial" font-size="16">0</text>

  <!-- *** Energy components illustration exactly at A/2 (x=350) *** -->
  <text x="338" y="345" font-family="Arial" font-size="14">A/2</text>
  
  <!-- Faint dashed guideline at A/2 -->
  <line x1="350" y1="120" x2="350" y2="320" stroke="#888" stroke-width="1" stroke-dasharray="3,3"/>

  <!-- Vertical energy bars (exactly on the x=350 axis meeting on the curve) -->
  <!-- Potential energy (Ep) segment - 50px long -->
  <line x1="350" y1="320" x2="350" y2="270" stroke="#00a000" stroke-width="4"/>
  <!-- Kinetic energy (Ek) segment - 150px long -->
  <line x1="350" y1="270" x2="350" y2="120" stroke="#ff9900" stroke-width="4"/>

  <!-- Small cross-bars for the energy segments for visibility -->
  <line x1="343" y1="320" x2="357" y2="320" stroke="#000" stroke-width="2"/>
  <line x1="343" y1="270" x2="357" y2="270" stroke="#000" stroke-width="2"/>
  <line x1="343" y1="120" x2="357" y2="120" stroke="#000" stroke-width="2"/>

  <!-- Intersection highlight on the blue curve -->
  <circle cx="350" cy="270" r="4.5" fill="#0055cc"/>

  <!-- Labels for the energy segments (shifted slightly right to prevent overlap) -->
  <text x="365" y="300" font-family="Arial" font-size="16" fill="#00a000">E<tspan dy="3" font-size="12">p</tspan></text>
  <text x="365" y="200" font-family="Arial" font-size="16" fill="#ff9900">E<tspan dy="3" font-size="12">k</tspan></text>

  <!-- Legend labels -->
  <text x="135" y="110" font-family="Arial" font-size="14" fill="#e60000">Total mechanical energy (E)</text>
  <text x="455" y="100" font-family="Arial" font-size="14" fill="#0055cc">Potential</text>
  <text x="455" y="118" font-family="Arial" font-size="14" fill="#0055cc">energy (E<tspan dy="2" font-size="10">p</tspan>)</text>
</svg>